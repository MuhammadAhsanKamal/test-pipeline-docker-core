apiVersion: v1
kind: Service
metadata:
    name: muhammadahsankamaltestpipelinedockercore
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: muhammadahsankamaltestpipelinedockercore